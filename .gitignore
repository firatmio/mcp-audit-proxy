# Build output
/bin/
/dist/
*.exe
*.test

# Local configuration and audit logs — never commit these, they contain tool
# arguments and can hold secrets.
/mcp-audit.yaml
/mcp-audit.yml
*.jsonl

# Go
/vendor/
coverage.out
.VSCodeCounter/

# Internal working documents. These stay on disk but out of the repository:
# they carry the business strategy — pricing, revenue targets, launch plan —
# and this repository is going public.
/CLAUDE.md
/PLAN.md
/launch/
